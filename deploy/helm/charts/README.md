# helm · charts

Helm charts for deploying the Bot Service Lite stack to Kubernetes. The `bot-service/` chart is the
full-stack chart (gateway + admin-api + meeting-api + runtime + infra); see `bot-service/` for values and
templates.
