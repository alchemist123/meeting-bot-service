{{- if .Values.adminApi.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bot-service.componentName" (list . "admin-api") }}
  labels:
    {{- include "bot-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.adminApi.service.type }}
  ports:
    - name: http
      port: {{ .Values.adminApi.service.port }}
      targetPort: http
  selector:
    {{- include "bot-service.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: admin-api
{{- end }}
