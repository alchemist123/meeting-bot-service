# buffer/src

Front door [`index.ts`](index.ts) re-exports the pure LocalAgreement-N core in
[`local-agreement.ts`](local-agreement.ts). `local-agreement.test.ts` is the
confirm-loop golden (`gate:node` runs it).
